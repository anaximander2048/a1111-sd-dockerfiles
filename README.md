#  a1111-sd-dockerfiles

## WIP project

A Dockerfile that will create a Docker Image meant to be executed over a wooten PC

A Dockerfile that will create a Docker Image meant to be executed over a normal PC with NVIDIA
